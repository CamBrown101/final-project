import React from 'react';
import { Draggable } from 'react-beautiful-dnd';

export default function DragItem({
  id,
  seat,
  orderItemId,
  price,
  name,
  key,
  draggableId,
  index,
}) {
  console.log(seat);
  return <div></div>;
}
